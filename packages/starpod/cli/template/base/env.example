# Contact form: Discord webhook the contact API posts to.
DISCORD_WEBHOOK=

# Guests & sponsors database (only with `database: true` in astro.config.mjs).
# Create a database at https://turso.tech and fill these in.
ASTRO_DB_REMOTE_URL=
ASTRO_DB_APP_TOKEN=
